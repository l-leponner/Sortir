<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">

        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
{#             <link rel="stylesheet" href ="{{ asset('CSS/base.css') }}">#}
        {% endblock %}

        {% block javascripts %}
{#            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js">#}
        {% endblock %}
    </head>
    {%block header%}

        <header>

            <h1 class="logo">Logo Sortir.com</h1>

            <ul>
                <li><a href="{{path('index')}}">Accueil</a></li>
                <li><a href="{{path('participant_profile')}}" class="sidenav-link">Mon profil</a></li>
                <li><a href="{{path('app_logout')}}" class="sidenav-link">Se déconnecter</a></li>
            </ul>
        </header>

    {%endblock%}
{%block body%}

    <body>
{# <a href="{{path('index')}}" title="Go back home"><img src="{{asset('img/logo.png')}}" alt="Logo Sortir" title="Logo Sortir"></a>#}

    {%for label,messages in app.flashes%}
        {%for message in messages%}
            <div class="alert-{{label}}">
                {{message}}
            </div>
        {%endfor%}
    {%endfor%}

    </body>

</html>
{% endblock %}